program is essentially a device connector that lets use do basic input stuff on a different device.
No, it probably isnt secure.

To add/remove saved name-ip combos from the file, refer to the console commands
While these names arent saved in lower case, they are read in lower case, so are not case sensitive

console commands:
all start with /

/add {name} {ip}
/rem {name}  # this is not case sensitive